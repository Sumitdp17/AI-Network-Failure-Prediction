# AI-Network-Failure-Prediction

Project scaffold for AI network failure prediction.

## Structure

- `data/` for raw and processed datasets
- `notebooks/` for exploratory work
- `src/` for application and pipeline code
- `dashboard/` for UI assets
- `mlruns/` for experiment tracking artifacts
- `models/` for trained model outputs
- `tests/` for automated tests
